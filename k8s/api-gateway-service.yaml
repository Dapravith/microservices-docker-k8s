---
apiVersion: v1
kind: Service
metadata:
  name: api-gateway-service
  namespace: msp
spec:
  type: NodePort
  selector:
    app: api-gateway
  ports:
    - name: http
      port: 4000
      targetPort: 4000
      nodePort: 30000
