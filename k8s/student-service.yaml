---
apiVersion: v1
kind: Service
metadata:
  name: student-service
  namespace: msp
spec:
  type: ClusterIP
  selector:
    app: student-service
  ports:
    - name: http
      port: 5000
      targetPort: 5000
